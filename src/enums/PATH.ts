export enum PATH {
  START = "/",
  LOGIN = "/login",
  DIALOGS = "/dialogs",
  PROFILE = "/profile",
  USERS = "/users",
  NEWS = "/news",
  MUSIC = "/music",
  SETTINGS = "/settings",
  NOT_FOUND = "/404",
  FRIENDS = "/friends",
}
